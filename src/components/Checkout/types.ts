type UserInfo = {
  firstName: string
  lastName: string
  email: string
  phoneNumber: string
}
type ShippingAddressFormData = {
  country: string;
  city: string;
  region: string;
  streetNumber: string;
  houseNumber: string;
  postalCode: string;
};

export {UserInfo,ShippingAddressFormData};
